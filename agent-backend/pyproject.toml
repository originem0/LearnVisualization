[project]
name = "learning-site-agent-backend"
version = "0.1.0"
description = "Zero-dependency dev server skeleton for Learning Site Engine agent backend"
requires-python = ">=3.11"
dependencies = []
