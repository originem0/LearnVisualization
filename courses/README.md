# Courses

这个目录用于放未来的 **Course Packages**。

每个专题课程应逐步收敛成独立包，例如：

```txt
courses/
  llm-fundamentals/
    course.json
    modules/
    visuals/
    interactions/
```

当前阶段：
- 现运行内容仍在 `src/content/zh`
- 这里先作为未来结构的目标位置
- 后续会先镜像一份 `llm-fundamentals` 课程包，再逐步接入运行时读取层
