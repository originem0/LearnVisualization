{
  "id": 12,
  "title": "系统化回顾",
  "subtitle": "将模型变成自己的知识",
  "category": "frontier",
  "concepts": {
    "items": [
      {
        "name": "完整推理流水线"
      },
      {
        "name": "模块间依赖关系"
      },
      {
        "name": "从零件到系统的整合"
      }
    ]
  },
  "quote": "最后一课是把知识组织成自己的语言",
  "keyInsight": "理解每个零件只是开始——真正的理解是能追踪一个 prompt 从输入到输出经历的每一步变换，并知道每步为什么这样做",
  "logicChain": [
    "文本输入 → 分词器将文本切分为 token（s01）",
    "Token ID → Embedding 矩阵查表得到语义向量 + 位置编码（s02）",
    "向量 → 注意力机制让 token 互相“看见”（s03）",
    "注意力输出 → Transformer Block 的残差/归一化/FFN 处理（s04）",
    "N 层堆叠后 → 输出投影到词表大小 → Softmax 得到概率分布（s05/s06）",
    "采样一个 token → 加入序列 → 重复（s08 Prompt 引导整个过程）",
    "模型的能力来自预训练（s05）+ 优化（s06）+ 对齐（s07），受规模（s09）、涌现（s10）、上下文（s11）影响"
  ],
  "examples": [
    "输入 ‘天空是’ → [22825, 31712, 1621] → 嵌入 → 96 层 Transformer → 概率分布 → 采样 ‘蓝’ → 继续生成 ‘色的’"
  ],
  "counterexamples": [
    "知道每个零件不等于理解系统——就像知道发动机、变速箱、轮胎的原理，不等于知道怎么开车"
  ],
  "opening": "你现在知道了 LLM 的每一个零件。但零件和机器是两回事。\n\n让我们把所有东西串起来——追踪一个 prompt 从输入到输出的完整旅程。如果你能在脑中走完这个旅程，不卡壳，不犹豫——那你就真正理解了大语言模型。",
  "narrative": [
    {
      "type": "heading",
      "content": "一个 prompt 的完整旅程"
    },
    {
      "type": "text",
      "content": "用户输入“天空是什么颜色？”。让我们追踪这段文字经历的每一步变换。\n\n第一站是分词器（s01）。BPE 算法将这段文字切分成 token：“天空”、“是”、“什么”、“颜色”、“？”。每个 token 对应一个整数 ID。文字消失了，剩下的是一串数字。\n\n第二站是 Embedding（s02）。每个 ID 从 Embedding 矩阵中查表取出一个 768 维（或更高）的向量。同时，RoPE 位置编码赋予每个向量“我在第几个位置”的信息。现在每个 token 有了语义和位置。\n\n第三站是注意力（s03）。通过 Q·K^T/√d → Softmax → ×V，每个 token “看到”了其他 token。“什么”看到了“天空”和“颜色”，开始理解这是一个关于天空颜色的问题。因果遮罩确保每个 token 只能看到前面的 token。\n\n第四站是 Transformer Block（s04）。注意力输出经过残差连接和层归一化保持稳定，再经过 FFN 做非线性变换。这样的 Block 重复 N 次（GPT-3 是 96 次）。每一层都在提炼更抽象的特征。"
    },
    {
      "type": "heading",
      "content": "从概率到文字"
    },
    {
      "type": "text",
      "content": "经过 N 层处理后，最后一个位置的输出向量被投影到词表大小的维度——50000 个数字，代表每个词作为下一个 token 的“原始分数”（logits）。\n\nSoftmax 将 logits 转换为概率分布。“蓝”可能得到 0.72 的概率，“红”0.03，“白”0.08……\n\n然后是采样：根据温度参数和 top-p 策略，从概率分布中选出一个 token。假设选中了“蓝”。\n\n“蓝”被添加到序列末尾，整个过程从头重复——现在输入是“天空是什么颜色？蓝”，生成下一个 token。如此反复，直到生成结束标记或达到长度限制。"
    },
    {
      "type": "heading",
      "content": "训练：这一切是怎么学会的"
    },
    {
      "type": "text",
      "content": "模型知道“天空”后面大概率是“蓝”，不是因为有人告诉它。这来自预训练（s05）——在几 TB 的文本上反复做“预测下一个词”。每次预测错误都产生损失，损失通过反向传播（s06）变成梯度，梯度驱动 Adam 优化器更新几十亿个参数。几十万步之后，模型的参数自动编码了语言的结构。\n\n预训练出来的模型只会续写。微调和对齐（s07）让它变成一个有用的助手——会回答问题、拒绝有害请求、遵循指令。\n\n最后，用户通过 Prompt（s08）引导模型：角色、上下文、示例——每个维度都在约束输出空间。"
    },
    {
      "type": "heading",
      "content": "全局视角"
    },
    {
      "type": "text",
      "content": "在这条主线之外，还有三个宏观视角：\n\n规模（s09）：参数量、数据量和算力之间遵循幂律关系。给定预算，Chinchilla 法则告诉你最优的资源分配。\n\n涌现（s10）：规模增长不只是“量变”——某些能力在临界点后突然出现。Chain-of-Thought 让模型外化推理过程，解锁了新的能力层级。\n\n上下文（s11）：模型一次能看到多少信息决定了它能处理多复杂的任务。注意力的 O(n²) 是根本瓶颈，各种优化技术在不断突破这个限制。\n\n12 个模块，一个系统。如果你能用自己的话讲清楚每一步——恭喜，你真正理解了大语言模型。"
    }
  ],
  "bridgeTo": null,
  "pitfalls": []
}
