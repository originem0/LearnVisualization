# Learning Site Engine Examples

这个目录放的是**工程草案级**示例，不直接参与当前网站运行。

用途：

- 验证 `Course Schema v1` 是否足够表达当前 LLM 样板课程
- 为后续 Agent structured output 提供参考格式
- 为 engine / course package 分层改造提供落地样例

当前文件：

- `llm-course-package.example.json` — 基于现有 LearnVisualization 抽出的最小课程包样例
