# Engine

这个目录是 `LearnVisualization` 未来抽象为 **Learning Site Engine** 后的目标位置。

当前阶段：
- 这里主要放引擎边界与结构说明
- 当前运行时代码仍然在 `src/` 下

原则：
- 不先搬一堆代码进来制造混乱
- 先明确 engine 与 course package 的职责边界
- 等课程包镜像与读取层稳定后，再考虑逐步抽离运行时代码
